namespace Piglet
{
    /// <summary>
    /// Indicates the format of binary image data (e.g. PNG, JPEG, KTX2).
    /// </summary>
    public enum ImageFormat
    {
        PNG,
        JPEG,
        KTX2,
        Unknown
    }
}